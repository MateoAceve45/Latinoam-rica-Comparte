function getProfile(req, res) {
  res.json({
    message: 'Perfil obtenido correctamente',
    user: req.user
  });
}

module.exports = {
  getProfile
};function getProfile(req, res) {
  res.json({
    message: 'Perfil obtenido correctamente',
    user: req.user
  });
}

module.exports = {
  getProfile
};